<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">

        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:ital,wght@0,100..700;1,100..700&display=swap" rel="stylesheet">

        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
        <link href="/modifyMovies.css" rel="stylesheet">
    </head>

    <body>

        <div class="createReview">
            <form class="reviewForm" action="/edit" method="POST">

                <input hidden value="<%= locals.movieId %>" name="movieId">

                <div class="mb-3">
                
                <input id="movieTitle" name="title" value="<%= locals.movieTitle %>" disabled>

                <div class="ratingOptions">
                    <label id="ratingLbl">Rating</label>
                    <svg xmlns="http://www.w3.org/2000/svg" width="35" height="35" fill="currentColor" class="bi bi-star-fill" viewBox="0 0 16 16">
                        <path d="M3.612 15.443c-.386.198-.824-.149-.746-.592l.83-4.73L.173 6.765c-.329-.314-.158-.888.283-.95l4.898-.696L7.538.792c.197-.39.73-.39.927 0l2.184 4.327 4.898.696c.441.062.612.636.282.95l-3.522 3.356.83 4.73c.078.443-.36.79-.746.592L8 13.187l-4.389 2.256z"/>
                    </svg>

                    <select name="rating" id="ratingSelection">
                        <!-- checks the original rating and set as selected by default -->
                        <option value="0" <% if (locals.movieRating === 0) { %>selected<% } %>>0</option>
                        <option value="1" <% if (locals.movieRating === 1) { %>selected<% } %>>1</option>
                        <option value="2" <% if (locals.movieRating === 2) { %>selected<% } %>>2</option>
                        <option value="3" <% if (locals.movieRating === 3) { %>selected<% } %>>3</option>
                        <option value="4" <% if (locals.movieRating === 4) { %>selected<% } %>>4</option>
                        <option value="5" <% if (locals.movieRating === 5) { %>selected<% } %>>5</option>
                    </select>

                </div>

                <div class="mb-3">                    
                <textarea id="ReviewContent" name="content"><%= locals.movieReview %></textarea>
                </div>

                <div class="saveBtnContainer"><button id="saveBtn" type="submit" class="btn btn-primary">Save</button></div>
            </form>
        </div>

    </body>
</html>